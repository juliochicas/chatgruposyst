#!/usr/bin/expect -f
set timeout 60
spawn ssh -o StrictHostKeyChecking=no root@62.72.7.2
expect {
  "password:" { send ".3+ml3&LKxGw6N1DbZd5\r" }
}
expect "#"
send "cd /root/chatgruposyst/codatendechat-main && docker compose run --rm --entrypoint sh backend\r"
expect "#"
send "npx sequelize db:migrate --debug 2>&1 | tail -30\r"
expect "#"
send "exit\r"
expect eof
